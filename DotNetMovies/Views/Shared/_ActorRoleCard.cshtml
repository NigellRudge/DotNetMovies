@model DotNetMovieCore.Models.ActorCastCredit

<div class="mt-8">
    <a asp-controller="Movies" asp-action="Details" asp-route-id="@Model.id">
        <img src="@Model.getPosterPath()" alt="actor1" class="hover:opacity-75 transition ease-in-out duration-150">
    </a>
    <div class="mt-2">
        <a href="#" class="text-lg mt-2 hover:text-gray:300">@Model.title</a>
        <div class="text-sm text-gray-400">
            @Model.character
        </div>
    </div>
</div>
